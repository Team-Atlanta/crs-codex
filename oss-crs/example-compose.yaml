##############################################################################
#                        CRS Compose Configuration                           #
#                     Example for crs-codex                            #
##############################################################################

# --- General Settings -------------------------------------------------------
run_env: local
docker_registry: local

# --- Infrastructure ---------------------------------------------------------
oss_crs_infra:
  cpuset: "0-1"
  memory: "8G"

# --- CRS (crs-codex) ---------------------------------------------------
# Builder sidecar is injected by the framework automatically.
crs-codex:
  source:
    local_path: /path/to/crs-codex                  # UPDATE: local filesystem path
  cpuset: "2-7"                                     # UPDATE: available CPU cores
  memory: "16G"                                     # UPDATE: available memory
  llm_budget: 10                                    # LLM budget in USD
  additional_env:
    # Override CRS defaults here.
    # Optional prepare-phase CLI pin:
    #   CODEX_CLI_VERSION: 0.104.0
    # Pinned snapshots (as of 2026-02-16):
    #   gpt-5-2025-08-07
    #   gpt-5-mini-2025-08-07
    #   gpt-5-pro-2025-10-06
    #   gpt-5.1-2025-11-13
    #   gpt-5.2-2025-12-11
    # Codex models currently use canonical IDs (no dated suffix published):
    #   gpt-5-codex, gpt-5.1-codex, gpt-5.1-codex-mini, gpt-5.2-codex, gpt-5.3-codex
    CRS_AGENT: codex
    CODEX_MODEL: gpt-5.4
    # AGENT_TIMEOUT: "3600"                          # Optional: seconds, 0 = no limit (default)

# --- Optional LLM Configuration --------------------------------------------
# Uncomment if you want OSS-CRS to inject an external LiteLLM endpoint.
# llm_config:
#   litellm:
#     mode: external
#     external:
#       url_env: EXTERNAL_LITELLM_API_BASE
#       key_env: EXTERNAL_LITELLM_API_KEY
